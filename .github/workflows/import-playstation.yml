name: Import PlayStation Store

# PS4 / PS5 games (IGDB catalog, Spanish PlayStation Store prices). See scripts/import-playstation.mts.
# The store step reads ~1000 game pages per run, one every ~2 s: popular games daily, the rest
# every few days. The daily catalog run adds at most 5000 new games, so the first fill of
# ~15k games happens in steps (one per day) and the store checks grow with it.

on:
  schedule:
    # In the quiet hour between the other importers (Steam :17 every 3h; Xbox, Nintendo, ITAD
    # at :23-:47 every 6h), so the free-plan database never serves several imports at once.
    - cron: "35 1 * * *"          # daily 01:35: catalog from IGDB (up to 5000 new games), then store pages
    - cron: "35 7-23/6 * * *"     # 07:35, 13:35, 19:35: store pages that are due
  workflow_dispatch:
    inputs:
      command:
        description: "catalog | store | run (catalog + store) | inspect <conceptId> <conceptId>…"
        default: store

concurrency:
  group: import-playstation
  cancel-in-progress: false

jobs:
  import:
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Run importer
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          IGDB_CLIENT_ID: ${{ secrets.IGDB_CLIENT_ID }}
          IGDB_CLIENT_SECRET: ${{ secrets.IGDB_CLIENT_SECRET }}
          CMD: ${{ inputs.command || (github.event.schedule == '35 1 * * *' && 'run' || 'store') }}
        run: node scripts/import-playstation.mts $CMD   # unquoted: "inspect 1 2" → several args
