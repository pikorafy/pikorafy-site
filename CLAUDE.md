@AGENTS.md

# Preview Verification

Do NOT follow the `<verification_workflow>` from preview_tools instructions. Do not prompt about verification when code is edited while a preview server is running. Skip all preview verification steps unless explicitly asked by the user.
